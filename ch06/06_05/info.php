this is an operator in php called spaceship operator.
denoted by <=>
it compares two objects and returns, -1 if left is less than right.

returns 0 if both are equal.
returns 1 if left is greater than right.